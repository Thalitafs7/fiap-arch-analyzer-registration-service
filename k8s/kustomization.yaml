apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: arch-analyzer-api

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - configmap.yaml
  - aws-secret-template.yaml
  # namespace.yaml omitted: arch-analyzer-api namespace is bootstrapped by the infra repo

commonLabels:
  app.kubernetes.io/part-of: arch-analyzer

images:
  # newName and newTag are substituted by the Deployment_Orchestrator at deploy time:
  #   kubectl kustomize k8s/ | \
  #     sed "s|\${ECR_REGISTRY}|<account>.dkr.ecr.us-east-1.amazonaws.com|g" | \
  #     sed "s|\${IMAGE_TAG}|<git-sha>|g" | kubectl apply -f -
  # Or via kustomize edit set image before apply:
  #   kustomize edit set image arch-analyzer-registration=${ECR_REGISTRY}/arch-analyzer-registration:${IMAGE_TAG}
  - name: arch-analyzer-registration
    newName: ${ECR_REGISTRY}/arch-analyzer-registration
    newTag: ${IMAGE_TAG}
